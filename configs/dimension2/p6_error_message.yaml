target:
  host: localhost
  http_port: 3000

proxy:
  http_port: 8080
  tcp_port: 2121

logging:
  log_dir: ./logs

defenses:
  - enabled: true
    objective: MISDIRECTION
    position: error_message
    trigger: context_blend
    payload: fake_flag
    mode: body
